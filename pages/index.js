import { useState, useEffect, useRef, useCallback } from 'react';
import Head from 'next/head';
function formatPhone(num) { if(!num)return''; const c=num.replace(/[^0-9]/g,''); if(c.length===11&&c.startsWith('1'))return'('+c.slice(1,4)+') '+c.slice(4,7)+'-'+c.slice(7); if(c.length===10)return'('+c.slice(0,3)+') '+c.slice(3,6)+'-'+c.slice(6); return num; }
function timeAgo(date) { const d=Math.floor((new Date()-new Date(date))/1000); if(d<60)return'just now'; if(d<3600)return Math.floor(d/60)+'m ago'; if(d<86400)return Math.floor(d/3600)+'h ago'; if(d<604800)return Math.floor(d/86400)+'d ago'; return new Date(date).toLocaleDateString('en-US',{month:'short',day:'numeric'}); }
function formatTs(date) { return new Date(date).toLocaleString('en-US',{timeZone:'America/New_York',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',hour12:true}); }
function ConvItem({conv,isActive,onClick}) { const u=conv.unread_count>0; return(<div onClick={onClick} style={{display:'flex',alignItems:'center',gap:12,padding:'14px 16px',cursor:'pointer',background:isActive?'var(--bg-tertiary)':'transparent',borderBottom:'1px solid var(--border)',borderLeft:isActive?'3px solid var(--accent)':'3px solid transparent'}} onMouseEnter={e=>{if(!isActive)e.currentTarget.style.background='var(--bg-hover)'}} onMouseLeave={e=>{if(!isActive)e.currentTarget.style.background='transparent'}}><div style={{width:42,height:42,borderRadius:'50%',flexShrink:0,background:u?'var(--accent)':'var(--bg-tertiary)',display:'flex',alignItems:'center',justifyContent:'center',fontSize:16,fontWeight:700,color:u?'var(--bg-primary)':'var(--text-secondary)'}}>{(conv.contact_name||'?')[0].toUpperCase()}</div><div style={{flex:1,minWidth:0}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:3}}><span style={{fontWeight:u?700:500,fontSize:14,color:'var(--text-primary)',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{conv.contact_name||formatPhone(conv.contact_number)}</span><span style={{fontSize:11,color:'var(--text-muted)',flexShrink:0,marginLeft:8}}>{timeAgo(conv.last_message_at)}</span></div><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span style={{fontSize:13,color:u?'var(--text-primary)':'var(--text-secondary)',fontWeight:u?500:400,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{conv.last_direction==='outbound'?'You: ':''}{conv.last_message}</span>{u&&<span style={{background:'var(--accent)',color:'var(--bg-primary)',fontSize:11,fontWeight:700,borderRadius:10,padding:'1px 7px',marginLeft:8,flexShrink:0}}>{conv.unread_count}</span>}</div></div></div>); }
function MsgBubble({msg}) { const o=msg.direction==='outbound'; const f=msg.status==='failed'||msg.status==='undelivered'; return(<div style={{display:'flex',justifyContent:o?'flex-end':'flex-start',marginBottom:8,animation:'fadeIn 0.2s ease'}}><div style={{maxWidth:'75%',padding:'10px 14px',borderRadius:16,borderBottomRightRadius:o?4:16,borderBottomLeftRadius:o?16:4,background:o?(f?'rgba(239,68,68,0.15)':'var(--accent-dim)'):'var(--bg-tertiary)',border:'1px solid '+(o?(f?'rgba(239,68,68,0.3)':'rgba(0,212,170,0.2)'):'var(--border)')}}><div style={{fontSize:14,lineHeight:1.5,color:'var(--text-primary)',wordBreak:'break-word'}}>{msg.body}</div><div style={{fontSize:11,color:'var(--text-muted)',marginTop:4,display:'flex',alignItems:'center',gap:6,justifyContent:o?'flex-end':'flex-start'}}>{formatTs(msg.created_at)}{o&&<span style={{color:f?'var(--red)':msg.status==='delivered'?'var(--green)':'var(--text-muted)'}}>{f?'! Failed':msg.status==='delivered'?'\u2713\u2713':'\u2713'}</span>}{msg.source&&msg.source!=='manual'&&msg.source!=='inbound'&&<span style={{fontSize:10,padding:'1px 5px',borderRadius:4,background:'var(--bg-hover)',color:'var(--text-muted)'}}>{msg.source}</span>}</div></div></div>); }
export default function SMSHub() {
  const [convs,setConvs]=useState([]); const [active,setActive]=useState(null); const [thread,setThread]=useState(null); const [reply,setReply]=useState(''); const [sending,setSending]=useState(false); const [loading,setLoading]=useState(true); const [search,setSearch]=useState(''); const [newNum,setNewNum]=useState(''); const [showNew,setShowNew]=useState(false); const endRef=useRef(null); const inputRef=useRef(null);
  const fetchConvs=useCallback(async()=>{try{const r=await fetch('/api/sms/conversations');const d=await r.json();if(d.conversations)setConvs(d.conversations);}catch(e){console.error(e);}finally{setLoading(false);}},[]);
  const fetchThread=useCallback(async(c)=>{try{const r=await fetch('/api/sms/thread?contact='+encodeURIComponent(c));setThread(await r.json());}catch(e){console.error(e);}},[]);
  useEffect(()=>{fetchConvs();const i=setInterval(fetchConvs,15000);return()=>clearInterval(i);},[fetchConvs]);
  useEffect(()=>{if(active){fetchThread(active);const i=setInterval(()=>fetchThread(active),10000);return()=>clearInterval(i);}},[active,fetchThread]);
  useEffect(()=>{endRef.current?.scrollIntoView({behavior:'smooth'});},[thread?.messages]);
  useEffect(()=>{if(active&&inputRef.current)setTimeout(()=>inputRef.current.focus(),100);},[active]);
  const handleSend=async()=>{if(!reply.trim()||!active||sending)return;setSending(true);try{const r=await fetch('/api/sms/send',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({to:active,body:reply.trim(),source:'manual'})});const d=await r.json();if(d.success){setReply('');await fetchThread(active);await fetchConvs();}else alert('Failed: '+(d.error||'Unknown'));}catch(e){alert('Error: '+e.message);}finally{setSending(false);}};
  const handleNew=()=>{if(!newNum.trim())return;let n=newNum.replace(/[^0-9+]/g,'');if(!n.startsWith('+')){if(n.startsWith('1')&&n.length===11)n='+'+n;else if(n.length===10)n='+1'+n;else{alert('Invalid number');return;}}setActive(n);setShowNew(false);setNewNum('');fetchThread(n);};
  const filtered=convs.filter(c=>{if(!search)return true;const q=search.toLowerCase();return(c.contact_name&&c.contact_name.toLowerCase().includes(q))||c.contact_number.includes(q)||formatPhone(c.contact_number).includes(q)||(c.last_message&&c.last_message.toLowerCase().includes(q));});
  const totalUnread=convs.reduce((s,c)=>s+c.unread_count,0);
  return(<><Head><title>ADEX SMS Hub {totalUnread>0?'('+totalUnread+')':''}</title><meta name="viewport" content="width=device-width,initial-scale=1"/></Head><div style={{display:'flex',height:'100vh',overflow:'hidden'}}><div style={{width:380,flexShrink:0,borderRight:'1px solid var(--border)',display:'flex',flexDirection:'column',background:'var(--bg-secondary)'}}><div style={{padding:'18px 16px 14px',borderBottom:'1px solid var(--border)',background:'var(--bg-primary)'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:12}}><div><h1 style={{fontSize:20,fontWeight:700,color:'var(--text-primary)',margin:0}}>ADEX<span style={{color:'var(--accent)'}}>.</span> SMS Hub</h1><span style={{fontSize:12,color:'var(--text-muted)'}}>{convs.length} conversations{totalUnread>0&&<span style={{color:'var(--accent)',fontWeight:600}}>{' \u00b7 '+totalUnread+' unread'}</span>}</span></div><button onClick={()=>setShowNew(!showNew)} style={{background:'var(--accent)',color:'var(--bg-primary)',border:'none',borderRadius:8,padding:'8px 14px',fontSize:13,fontWeight:600,cursor:'pointer'}}>+ New</button></div>{showNew&&<div style={{display:'flex',gap:8,marginBottom:10,animation:'fadeIn 0.2s ease'}}><input type="text" placeholder="Enter phone number..." value={newNum} onChange={e=>setNewNum(e.target.value)} onKeyDown={e=>e.key==='Enter'&&handleNew()} style={{flex:1,padding:'8px 12px',borderRadius:6,border:'1px solid var(--border-light)',background:'var(--bg-tertiary)',color:'var(--text-primary)',fontSize:13,outline:'none'}}/><button onClick={handleNew} style={{background:'var(--accent)',color:'var(--bg-primary)',border:'none',borderRadius:6,padding:'8px 12px',fontSize:13,fontWeight:600,cursor:'pointer'}}>Go</button></div>}<input type="text" placeholder="Search conversations..." value={search} onChange={e=>setSearch(e.target.value)} style={{width:'100%',padding:'9px 12px',borderRadius:8,border:'1px solid var(--border)',background:'var(--bg-tertiary)',color:'var(--text-primary)',fontSize:13,outline:'none'}}/></div><div style={{flex:1,overflowY:'auto'}}>{loading?<div style={{padding:40,textAlign:'center',color:'var(--text-muted)'}}><div style={{animation:'pulse 1.5s infinite'}}>Loading...</div></div>:filtered.length===0?<div style={{padding:40,textAlign:'center',color:'var(--text-muted)',fontSize:14}}>{search?'No matches':'No conversations yet'}</div>:filtered.map(c=><ConvItem key={c.contact_number} conv={c} isActive={active===c.contact_number} onClick={()=>setActive(c.contact_number)}/>)}</div></div><div style={{flex:1,display:'flex',flexDirection:'column',background:'var(--bg-primary)'}}>{!active?<div style={{flex:1,display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',color:'var(--text-muted)'}}><div style={{fontSize:48,marginBottom:16,opacity:0.3}}>💬</div><div style={{fontSize:16,fontWeight:500}}>Select a conversation</div><div style={{fontSize:13,marginTop:4}}>or start a new one</div></div>:<><div style={{padding:'14px 20px',borderBottom:'1px solid var(--border)',background:'var(--bg-secondary)',display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div style={{fontSize:16,fontWeight:600,color:'var(--text-primary)'}}>{thread?.contact_name||formatPhone(active)}</div><div style={{fontSize:12,color:'var(--text-muted)',marginTop:2}}>{formatPhone(active)}{thread?.contact_email&&' \u00b7 '+thread.contact_email}{thread?.messages&&' \u00b7 '+thread.messages.length+' messages'}</div></div><button onClick={()=>{setActive(null);setThread(null);}} style={{background:'none',border:'1px solid var(--border)',color:'var(--text-secondary)',borderRadius:6,padding:'6px 12px',fontSize:12,cursor:'pointer'}}>Close</button></div><div style={{flex:1,overflowY:'auto',padding:'16px 20px'}}>{thread?.messages?.length===0?<div style={{textAlign:'center',color:'var(--text-muted)',padding:40,fontSize:14}}>No messages yet. Send the first one below.</div>:thread?.messages?.map(m=><MsgBubble key={m.id} msg={m}/>)}<div ref={endRef}/></div><div style={{padding:'12px 20px',borderTop:'1px solid var(--border)',background:'var(--bg-secondary)',display:'flex',gap:10,alignItems:'flex-end'}}><textarea ref={inputRef} value={reply} onChange={e=>setReply(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();handleSend();}}} placeholder="Type a message... (Enter to send)" rows={1} style={{flex:1,padding:'10px 14px',borderRadius:10,border:'1px solid var(--border-light)',background:'var(--bg-tertiary)',color:'var(--text-primary)',fontSize:14,outline:'none',resize:'none',fontFamily:'inherit',lineHeight:1.4,maxHeight:120,overflowY:'auto'}} onInput={e=>{e.target.style.height='auto';e.target.style.height=Math.min(e.target.scrollHeight,120)+'px';}}/><button onClick={handleSend} disabled={sending||!reply.trim()} style={{background:reply.trim()?'var(--accent)':'var(--bg-tertiary)',color:reply.trim()?'var(--bg-primary)':'var(--text-muted)',border:'none',borderRadius:10,padding:'10px 20px',fontSize:14,fontWeight:600,cursor:reply.trim()?'pointer':'default',opacity:sending?0.6:1,flexShrink:0}}>{sending?'Sending...':'Send'}</button></div></>}</div></div></>);
}
